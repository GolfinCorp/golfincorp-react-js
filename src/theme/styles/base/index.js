import Button from './buttons';
import IconButton from './iconButton';
import Input from './input';
import Badge from './badge';

const baseComponents = { Button, IconButton, Input, Badge };

export default baseComponents;
